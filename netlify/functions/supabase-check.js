const {db,bucket}=require('./_shared/supabase');const {admin}=require('./_shared/auth');const {json,fail}=require('./_shared/response');exports.handler=async(event,context)=>{try{admin(context);const {count,error}=await db().from('orders').select('*',{count:'exact',head:true});if(error)throw error;return json(200,{ok:true,orders_count:count,bucket:bucket()});}catch(err){return fail(err);}};
